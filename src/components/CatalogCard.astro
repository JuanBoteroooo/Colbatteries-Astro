---
interface Detalles {
  dimensiones?:  string;
  altura?:       string;
  joyas?:        number;
  bateria?:      string;
  placas?:       string;
  intercambios?: string;
}

interface Props {
  imagenSrc:          string;
  imagenCatalogoSrc:  string;
  titulo:             string;
  detalles:           Detalles;
  tipo?:              string;
  fecha?:             boolean;
  isCorona?:          boolean;
  isWatch?:           boolean;
  isVidrios?:         boolean;
  isModulos?:         boolean;
  isInsumos?:         boolean;
  isHerramientas?:    boolean;
  isJoyeria?:         boolean;
  isGorras?:          boolean;
  rosca?:             string;
  medida?:            string;
  color?:             string;
  grosor?:            string;
  forma?:             string;
  nombre?:            string;
  marca?:             string;
  especificacion?:    string;
}

const {
  imagenSrc, imagenCatalogoSrc, titulo, detalles,
  tipo, fecha,
  isCorona = false, isWatch = false, isVidrios = false, isModulos = false, isInsumos = false, isHerramientas = false, isJoyeria = false, isGorras = false,
  rosca, medida, color, grosor, forma, nombre, marca, especificacion,
} = Astro.props;

const BADGE_CLASS: Record<string, string> = {
  'CRONÓGRAFO':       'badge-crono',
  'CABALLERO':        'badge-cab',
  'DAMA':             'badge-dama',
  'CON ROSCA':        'badge-rosca',
  'CON ROSCA Y TUBO': 'badge-tubo',
  'ECONÓMICAS':       'badge-eco',
  'DIGITAL':          'badge-digital',
  'ACCESORIO':        'badge-acc',
  'PLANO':            'badge-vidrio-plano',
  'LUPA':             'badge-vidrio-lupa',
  'ZAFIRADO':         'badge-vidrio-zaf',
  'ESPECIAL':         'badge-vidrio-esp',
  'PEGANTE':          'badge-vidrio-peg',
  'ACRILICO':         'badge-vidrio-acr',
  'CONCAVO':          'badge-vidrio-plano',
  'LAMINA':           'badge-vidrio-plano',
  'DOMO':             'badge-vidrio-lupa',
  'OTRO':             'badge-acc',
  'SALTO':            'badge-mod-salto',
  'PENDULO':          'badge-mod-pen',
  'MUSICAL':          'badge-mod-mus',
  'FUERZA':           'badge-mod-fuerza',
  'EMPOTRABLE':       'badge-mod-emp',
  'PINS':             'badge-ins-pins',
  'EMPAQUES':         'badge-ins-emp',
  'CHAPETAS':         'badge-ins-cha',
  'HEBILLAS':         'badge-ins-heb',
  'QUIMICOS':         'badge-ins-qui',
  'TORNILLOS':        'badge-ins-tor',
  'TERMINALES':       'badge-ins-ter',
  'TIJAS':            'badge-ins-tij',
  'CORONAS':          'badge-ins-cor',
  'REPUESTOS':        'badge-ins-rep',
  'OTROS':            'badge-acc',
  'TAPADORA':         'badge-her-tap',
  'DESTAPADORA':      'badge-her-des',
  'PINZA':            'badge-her-pin',
  'DESTORNILLADOR':   'badge-her-dst',
  'MAQUINA':          'badge-her-maq',
  'LUPA':             'badge-her-lup',
  'BASE':             'badge-her-bas',
  'PRECISION':        'badge-her-pre',
  'LLAVE':            'badge-her-lla',
  'HERRAMIENTA':      'badge-her-her',
  'LIMA':             'badge-joy-lima',
  'SIERRA':           'badge-joy-sie',
  'CEPILLO':          'badge-joy-cep',
  'ABRASIVO':         'badge-joy-abr',
  'MANDRIL':          'badge-joy-man',
  'QUIMICO':          'badge-joy-qui',
  'CAMIONERA':        'badge-gor-cam',
  'ECONOMICA':        'badge-gor-eco',
  'HEBILLA':          'badge-gor-heb',
  'BORDADA':          'badge-gor-bor',
  'FLEX':             'badge-gor-fle',
  'PLANA':            'badge-gor-pla',
  'IMPERMEABLE':      'badge-gor-imp',
  'DAMA':             'badge-gor-dam',
  'GOLEANA':          'badge-gor-gol',
  'BOINA':            'badge-gor-boi',
  'NINO':             'badge-gor-nin',
  'NINA':             'badge-gor-nina',
  'CUERINA':          'badge-gor-cue',
  'DRIL':             'badge-gor-dri',
  'MALLA':            'badge-gor-mal',
  'MALLA-NINO':       'badge-gor-mni',
};

const TIPO_LABEL: Record<string, string> = {
  'CRONÓGRAFO':       'Cronógrafo',
  'CABALLERO':        'Caballero',
  'DAMA':             'Dama',
  'CON ROSCA':        'Con Rosca',
  'CON ROSCA Y TUBO': 'Rosca y Tubo',
  'ECONÓMICAS':       'Económica',
  'DIGITAL':          'Digital',
  'ACCESORIO':        'Accesorio',
  'SALTO':            'Salto',
  'PENDULO':          'Péndulo',
  'MUSICAL':          'Musical',
  'FUERZA':           'Fuerza',
  'EMPOTRABLE':       'Empotrable',
  'PINS':             'Pins',
  'EMPAQUES':         'Empaques',
  'CHAPETAS':         'Chapetas',
  'HEBILLAS':         'Hebillas',
  'QUIMICOS':         'Químicos',
  'TORNILLOS':        'Tornillos',
  'TERMINALES':       'Terminales',
  'TIJAS':            'Tijas',
  'CORONAS':          'Coronas',
  'REPUESTOS':        'Repuestos',
  'OTROS':            'Otros',
  'TAPADORA':         'Tapadora',
  'DESTAPADORA':      'Destapadora',
  'PINZA':            'Pinza',
  'DESTORNILLADOR':   'Destornillador',
  'MAQUINA':          'Máquina',
  'LUPA':             'Lupa',
  'BASE':             'Base',
  'PRECISION':        'Precisión',
  'LLAVE':            'Llave',
  'HERRAMIENTA':      'Herramienta',
  'LIMA':             'Lima',
  'SIERRA':           'Sierra',
  'CEPILLO':          'Cepillo',
  'ABRASIVO':         'Abrasivo',
  'MANDRIL':          'Mandril',
  'QUIMICO':          'Químico',
  'CAMIONERA':        'Camionera',
  'ECONOMICA':        'Económica',
  'HEBILLA':          'Hebilla',
  'BORDADA':          'Bordada',
  'FLEX':             'Flex',
  'PLANA':            'Plana',
  'IMPERMEABLE':      'Impermeable',
  'DAMA':             'Dama',
  'GOLEANA':          'Goleana',
  'BOINA':            'Boina',
  'NINO':             'Niño',
  'NINA':             'Niña',
  'CUERINA':          'Cuerina',
};

// Title split — for movements: "ETA 251.262" → prefix "ETA", model "251.262"
// For coronas/vidrios/modulos: titulo IS the ref, prefix is descriptive name
const spaceIdx = titulo.indexOf(' ');
const prefix   = isCorona
  ? (TIPO_LABEL[tipo ?? ''] ?? '')
  : isGorras
  ? (TIPO_LABEL[tipo ?? ''] ?? '')
  : (isVidrios || isModulos || isInsumos || isHerramientas || isJoyeria)
  ? (nombre ?? '')
  : (spaceIdx > -1 ? titulo.slice(0, spaceIdx) : '');
const modelNum = isCorona || isVidrios || isModulos || isInsumos || isHerramientas || isJoyeria || isGorras
  ? titulo
  : (spaceIdx > -1 ? titulo.slice(spaceIdx + 1) : titulo);

type SpecItem = { k: string; v: string };

const specs: SpecItem[] = isCorona
  ? [
      medida ? { k: 'Medida', v: medida.replace(/\s*[Xx]\s*/g, ' × ') } : null,
      rosca  ? { k: 'Rosca',  v: rosca } : null,
    ].filter((s): s is SpecItem => s !== null)
  : isVidrios
  ? [
      medida ? { k: 'Medida', v: medida } : null,
      grosor ? { k: 'Grosor', v: grosor } : null,
      forma  ? { k: 'Tipo',   v: forma.charAt(0) + forma.slice(1).toLowerCase() } : null,
    ].filter((s): s is SpecItem => s !== null)
  : isModulos
  ? [
      detalles.bateria     ? { k: 'Batería',    v: `Pila ${detalles.bateria}` } : null,
      detalles.dimensiones ? { k: 'Eje',        v: detalles.dimensiones }       : null,
      tipo                 ? { k: 'Tipo',       v: TIPO_LABEL[tipo] ?? tipo }    : null,
    ].filter((s): s is SpecItem => s !== null)
  : isInsumos
  ? [
      marca         ? { k: 'Marca',  v: marca }                          : null,
      especificacion ? { k: 'Espec.', v: especificacion.slice(0, 40) }   : null,
      tipo           ? { k: 'Cat.',   v: TIPO_LABEL[tipo] ?? tipo }       : null,
    ].filter((s): s is SpecItem => s !== null)
  : isHerramientas
  ? [
      tipo ? { k: 'Tipo', v: TIPO_LABEL[tipo] ?? tipo } : null,
    ].filter((s): s is SpecItem => s !== null)
  : isJoyeria
  ? [
      tipo ? { k: 'Tipo', v: TIPO_LABEL[tipo] ?? tipo } : null,
    ].filter((s): s is SpecItem => s !== null)
  : isGorras
  ? []
  : isWatch
  ? [
      tipo ? { k: 'Tipo', v: TIPO_LABEL[tipo] ?? tipo } : null,
    ].filter((s): s is SpecItem => s !== null)
  : [
      detalles.dimensiones      ? { k: 'Dimensiones', v: detalles.dimensiones }      : null,
      detalles.altura           ? { k: 'Altura',      v: `${detalles.altura}mm` }    : null,
      detalles.joyas != null    ? { k: 'Joyas',       v: String(detalles.joyas) }    : null,
      detalles.bateria          ? { k: 'Batería',     v: `CR ${detalles.bateria}` }  : null,
    ].filter((s): s is SpecItem => s !== null).slice(0, 4);

const SWATCH: Record<string, string> = {
  'PLATEADA': '#C0C0C8',
  'DORADA':   '#D4AF37',
  'NEGRA':    '#3a3a3a',
  'ORO ROSA': '#C88898',
};

const coronaColors = isCorona && color
  ? color.split(', ').map(c => c.trim())
  : [];
---

<article class="cc" data-img={imagenSrc} data-catalogo-src={imagenCatalogoSrc} data-title={titulo}>
  <!-- Image zone -->
  <div class="cc-img-zone">
    <img
      src={imagenSrc}
      alt=""
      class="cc-img"
      loading="lazy"
      decoding="async"
    />
    <!-- WA cotizar -->
    <a
      class="cc-wa"
      href={`https://wa.me/50764156382?text=${encodeURIComponent(`Hola, quisiera cotizar: ${titulo}`)}`}
      target="_blank"
      rel="noopener"
      aria-label="Cotizar por WhatsApp"
    >
      <svg width="11" height="11" viewBox="0 0 24 24" fill="currentColor">
        <path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893a11.821 11.821 0 00-3.48-8.413z"/>
      </svg>
      Cotizar
    </a>

    <!-- Badges -->
    <div class="cc-badge-strip">
      {isVidrios && forma ? (
        <span class={`cc-badge ${BADGE_CLASS[forma] ?? 'badge-acc'}`}>
          {forma.charAt(0) + forma.slice(1).toLowerCase()}
        </span>
      ) : tipo ? (
        <span class={`cc-badge ${BADGE_CLASS[tipo] ?? 'badge-cab'}`}>
          {TIPO_LABEL[tipo] ?? tipo}
        </span>
      ) : null}
      {!isCorona && !isVidrios && fecha && <span class="cc-badge badge-fecha">+ Fecha</span>}
    </div>
  </div>

  <!-- Info zone -->
  <div class="cc-body">
    <div class="cc-header">
      {prefix && <span class="cc-prefix">{prefix}</span>}
      <h3 class="cc-modelo">{modelNum}</h3>
    </div>

    {specs.length > 0 && (
      <div class="cc-rule"></div>
      <dl class="cc-specs">
        {specs.map(s => (
          <div class="cc-spec">
            <dd class="spec-v">{s.v}</dd>
            <dt class="spec-k">{s.k}</dt>
          </div>
        ))}
      </dl>
    )}

    {isCorona && coronaColors.length > 0 && (
      <div class="cc-swatches">
        {coronaColors.map(c => (
          <span
            class="cc-swatch"
            title={c}
            style={`background:${SWATCH[c] ?? '#888'}`}
          ></span>
        ))}
      </div>
    )}

    {!isCorona && detalles.intercambios && (
      <p class="cc-interchange">↔ {detalles.intercambios}</p>
    )}
  </div>
</article>

<style>
  /* ── Card shell ── */
  .cc {
    background: #fff;
    display: flex;
    flex-direction: column;
    overflow: hidden;
    border: 1px solid #dce4f0;
    border-radius: 10px;
    box-shadow: 0 2px 8px rgba(2, 69, 152, 0.06);
    transition: border-color 0.2s ease, box-shadow 0.2s ease, transform 0.2s ease;
    height: 100%;
  }

  /* Force all children to be unselectabe/unblocking — clicks pass through to .cc */
  .cc,
  .cc * {
    cursor: pointer !important;
    -webkit-user-select: none !important;
    -moz-user-select: none !important;
    -ms-user-select: none !important;
    user-select: none !important;
    -webkit-touch-callout: none !important;
  }

  .cc * {
    pointer-events: none !important;
  }

  .cc:hover {
    border-color: #f2a900;
    box-shadow:
      0 0 0 1.5px rgba(242,169,0,0.35),
      0 8px 28px rgba(242,169,0,0.14),
      0 2px 10px rgba(2,69,152,0.1);
    transform: translateY(-4px);
    outline: none;
  }

  /* ── Image zone ── */
  .cc-img-zone {
    position: relative;
    background: #f5f7fb;
    width: 100%;
    height: 200px;
    overflow: hidden;
    display: flex;
    align-items: center;
    justify-content: center;
    border-bottom: 1px solid #eef2f8;
    flex-shrink: 0;
  }

  .cc-img {
    width: 100%;
    height: 100%;
    object-fit: contain;
    display: block;
    padding: 1rem;
    transition: transform 0.35s ease;
  }

  .cc:hover .cc-img {
    transform: scale(1.06);
  }

  /* ── WA cotizar button ── */
  .cc-wa {
    position: absolute;
    top: 0.5rem;
    right: 0.5rem;
    display: inline-flex;
    align-items: center;
    gap: 0.28rem;
    padding: 0.28rem 0.6rem;
    background: #25D366;
    color: #fff;
    font-size: 0.58rem;
    font-weight: 700;
    border-radius: 100px;
    text-decoration: none;
    opacity: 0;
    transform: translateY(-5px) scale(0.9);
    transition: opacity 0.22s ease, transform 0.25s cubic-bezier(0.22,1,0.36,1), box-shadow 0.22s;
    pointer-events: auto !important;
    z-index: 3;
    letter-spacing: 0.03em;
    white-space: nowrap;
  }

  .cc:hover .cc-wa {
    opacity: 1;
    transform: translateY(0) scale(1);
    box-shadow: 0 2px 10px rgba(37,211,102,0.45);
  }

  /* ── Badge strip ── */
  .cc-badge-strip {
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    display: flex;
    gap: 0.3rem;
    padding: 0.45rem 0.6rem;
    background: linear-gradient(to top, rgba(245, 247, 251, 0.95), transparent);
    flex-wrap: wrap;
  }

  .cc-badge {
    display: inline-block;
    font-family: var(--font-body);
    font-size: 0.5rem;
    font-weight: 900;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    padding: 0.22rem 0.6rem;
    border-radius: 3px;
    line-height: 1.5;
  }

  .badge-crono { background: #f2a900; color: #111; }
  .badge-cab   { background: #024598; color: #fff; }
  .badge-dama  { background: #c8245a; color: #fff; }
  .badge-fecha {
    background: rgba(2, 69, 152, 0.08);
    color: #024598;
    border: 1px solid rgba(2, 69, 152, 0.18);
  }
  /* Corona badges */
  .badge-rosca    { background: #f2a900; color: #111; }
  .badge-tubo     { background: #024598; color: #fff; }
  .badge-eco      { background: #e64132; color: #fff; }
  .badge-digital  { background: #0a8a50; color: #fff; }
  .badge-acc      { background: #777;    color: #fff; }
  /* Vidrios badges */
  .badge-vidrio-plano { background: #4a7fb5; color: #fff; }
  .badge-vidrio-lupa  { background: #7b5ea7; color: #fff; }
  .badge-vidrio-zaf   { background: #1a7a5e; color: #fff; }
  .badge-vidrio-esp   { background: #c56a1e; color: #fff; }
  .badge-vidrio-peg   { background: #666;    color: #fff; }
  .badge-vidrio-acr   { background: #3a8fa8; color: #fff; }
  /* Módulos badges */
  .badge-mod-salto    { background: #024598; color: #fff; }
  .badge-mod-pen      { background: #5a3d8a; color: #fff; }
  .badge-mod-mus      { background: #c56a1e; color: #fff; }
  .badge-mod-fuerza   { background: #c0392b; color: #fff; }
  .badge-mod-emp      { background: #1a7a5e; color: #fff; }
  /* Insumos badges */
  .badge-ins-pins  { background: #024598; color: #fff; }
  .badge-ins-emp   { background: #2e7d5e; color: #fff; }
  .badge-ins-cha   { background: #7b5ea7; color: #fff; }
  .badge-ins-heb   { background: #a05195; color: #fff; }
  .badge-ins-qui   { background: #c0392b; color: #fff; }
  .badge-ins-tor   { background: #555;    color: #fff; }
  .badge-ins-ter   { background: #c56a1e; color: #fff; }
  .badge-ins-tij   { background: #1a6fa8; color: #fff; }
  .badge-ins-cor   { background: #d4af37; color: #111; }
  .badge-ins-rep   { background: #4a7fb5; color: #fff; }

  /* ── Herramientas badges ── */
  .badge-her-tap { background: #c0392b; color: #fff; }
  .badge-her-des { background: #e74c3c; color: #fff; }
  .badge-her-pin { background: #16a085; color: #fff; }
  .badge-her-dst { background: #2980b9; color: #fff; }
  .badge-her-maq { background: #6c3483; color: #fff; }
  .badge-her-lup { background: #d4ac0d; color: #111; }
  .badge-her-bas { background: #7f8c8d; color: #fff; }
  .badge-her-pre { background: #0097a7; color: #fff; }
  .badge-her-lla { background: #795548; color: #fff; }
  .badge-her-her { background: #546e7a; color: #fff; }

  /* ── Joyería badges ── */
  .badge-joy-lima { background: #b7410e; color: #fff; }
  .badge-joy-sie  { background: #2471a3; color: #fff; }
  .badge-joy-cep  { background: #1e8449; color: #fff; }
  .badge-joy-abr  { background: #7d6608; color: #fff; }
  .badge-joy-man  { background: #4a235a; color: #fff; }
  .badge-joy-qui  { background: #784212; color: #fff; }

  /* ── Gorras Polemik badges ── */
  .badge-gor-cam  { background: #024598; color: #fff; }
  .badge-gor-eco  { background: #2e7d5e; color: #fff; }
  .badge-gor-heb  { background: #6c3483; color: #fff; }
  .badge-gor-bor  { background: #c0392b; color: #fff; }
  .badge-gor-fle  { background: #0097a7; color: #fff; }
  .badge-gor-pla  { background: #546e7a; color: #fff; }
  .badge-gor-imp  { background: #1565c0; color: #fff; }
  .badge-gor-dam  { background: #ad1457; color: #fff; }
  .badge-gor-gol  { background: #558b2f; color: #fff; }
  .badge-gor-boi  { background: #4e342e; color: #fff; }
  .badge-gor-nin  { background: #1565c0; color: #fff; }
  .badge-gor-nina { background: #ad1457; color: #fff; }
  .badge-gor-cue  { background: #795548; color: #fff; }
  .badge-gor-dri  { background: #5d4037; color: #fff; }
  .badge-gor-mal  { background: #1565c0; color: #fff; }
  .badge-gor-mni  { background: #283593; color: #fff; }

  /* ── Body ── */
  .cc-body {
    padding: 1rem 1.1rem 1rem;
    flex: 1;
    display: flex;
    flex-direction: column;
    gap: 0;
  }

  .cc-header {
    display: flex;
    flex-direction: column;
    gap: 0.05rem;
    margin-bottom: 0.55rem;
  }

  .cc-prefix {
    font-family: var(--font-body);
    font-size: 0.6rem;
    font-weight: 600;
    color: #9aabbf;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    line-height: 1;
  }

  .cc-modelo {
    font-family: var(--font-heading);
    font-size: clamp(1rem, 2.2vw, 1.15rem);
    font-weight: 800;
    color: #024598;
    letter-spacing: -0.01em;
    line-height: 1.1;
    margin: 0;
  }

  /* ── Hairline rule ── */
  .cc-rule {
    height: 1px;
    background: #eef2f8;
    margin-bottom: 0.65rem;
  }

  /* ── Specs grid ── */
  .cc-specs {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 0.5rem 0.4rem;
    margin: 0;
  }

  .cc-spec {
    display: flex;
    flex-direction: column;
    gap: 0.04rem;
  }

  .spec-v {
    font-family: var(--font-body);
    font-size: 0.72rem;
    font-weight: 500;
    color: #1a2a4a;
    line-height: 1.2;
    margin: 0;
  }

  .spec-k {
    font-family: var(--font-body);
    font-size: 0.52rem;
    font-weight: 600;
    color: #9aabbf;
    text-transform: uppercase;
    letter-spacing: 0.07em;
  }

  /* ── Interchange line ── */
  .cc-interchange {
    margin-top: 0.55rem;
    font-family: var(--font-body);
    font-size: 0.58rem;
    font-weight: 400;
    color: #9aabbf;
    line-height: 1.4;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  /* ── Corona color swatches ── */
  .cc-swatches {
    display: flex;
    gap: 0.35rem;
    flex-wrap: wrap;
    margin-top: 0.6rem;
  }

  .cc-swatch {
    width: 14px;
    height: 14px;
    border-radius: 50%;
    border: 1.5px solid rgba(0, 0, 0, 0.12);
    flex-shrink: 0;
  }
</style>
